<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.auction.Dao.AuctionInfoCarImgDao">

  <select id="getById" resultType="com.example.auction.Model.AuctionInfoCarImg">
    select
    *
    from auctioninfo_car_img
    where auctioninfoid = #{auctioninfoid}
  </select>

  <insert id="insertInfo" parameterType="com.example.auction.Model.AuctionInfoCarImg">
    insert into auctioninfo_car_img (auctioninfoid,bidderid, carid, carimg,usernumber)
    values (#{auctioninfoid,jdbcType=INTEGER}, #{bidderid,jdbcType=INTEGER}, #{carid,jdbcType=INTEGER}, #{carimg,jdbcType=VARCHAR}, #{usernumber,jdbcType=VARCHAR})
  </insert>
  <update id="updateInfo" parameterType="com.example.auction.Model.AuctionInfoCarImg">
    update auctioninfo_car_img
    <set>
      <if test="carimg != null">
        carimg = #{carimg},
      </if>
    </set>
    <where>
      <if test="auctioninfoid != null">
        auctioninfoid=#{auctioninfoid}
      </if>
      <if test="bidderid != null">
        and bidderid=#{bidderid}
      </if>
      <if test="carid != null">
        and carid=#{carid}
      </if>
      <if test="usernumber != null">
        and usernumber=#{usernumber}
      </if>
    </where>
  </update>

</mapper>